# 独立上下文审计报告（两轮）

审计方式：把代码交给**全新上下文、未参与编写**的审计代理，只读、禁止修改文件，要求给出 `文件:行号` 级别的证据，并按 P0 / P1 / P2 分级。

- **P0**：功能不完整、崩溃、明显 bug、与需求不符 —— 必须修，修完要再开一轮审计
- **P1**：边界情况错误、设计缺陷、测试缺口 —— 应当修
- **P2**：风格、可读性、微小优化

两轮结束后 **P0 = 0**，用户要求的"直到没有 P0 为止"条件达成。

---

## 第一轮：3 个审计代理并行（逻辑 / 前端 / 冗余）

| 审计员 | 结论 |
| --- | --- |
| 逻辑与正确性 | P0 ×2，P1 ×3，P2 ×5 |
| 前端与体验 | P0 ×0，P1 ×4，P2 ×10 |
| 冗余与架构 | P0 ×0，P1 ×4，P2 ×11 |

### 确认为真并已修复的 P0

| # | 问题 | 证据（修复前） | 根因 | 修复 |
| --- | --- | --- | --- | --- |
| 1 | **加速完全不消耗质量** —— 需求写明的"用质量换速度"从未实现，按住加速可永久白嫖 1.6× 速度 | `config.js` 定义了 `boostMassCost: 9` 却全仓无读取方；`snake.js` 的 `step()` 只改速度不扣质量 | 定义了配置项但没接线 | `snake.js` `step()`：boosting 时 `mass -= boostMassCost * dt`，触底 `minMassToBoost` 自动松开；补 3 个回归测试 |
| 2 | **护盾道具是纯装饰** —— 拿了不会免死，碰撞判定从不检查它 | `_resolveCollisions()` 只看 `ghost` 与自撞豁免 | 同上，`effects.shield` 被写但从未被读 | `snake.js` 新增 `consumeShield()`（消耗充能 + 0.9s 无敌帧 `grace`）；`_resolveCollisions()` 四种死因统一先尝试护盾；补 2 个测试 |
| 3 | **触屏完全无法转向（手机不可玩）** —— 输入层只在 `pointerType === 'mouse'` 时才切到指针模式，手机玩家永远停在键盘模式、`steerAxis` 恒为 0，蛇只会直走 | `input.js` 的 `onPointerMove/onPointerDown` 里有 `if (e.pointerType === 'mouse')` 才切模式 | 触屏路径漏了 | 触摸事件立即接管转向；死区改为屏幕像素比较；补端到端断言 |
| 4 | **`globalAlpha` 泄漏 → 蛇头/宝石/道具核心半透明** | `glow.js` 的 `drawGlow()` 只赋值不复位 `globalAlpha`，调用方随后的实心填充全部以 0.34~0.55 的 alpha 绘制 | 未恢复画布状态 | `drawGlow()` 保存并还原 `globalAlpha` |
| 5 | **`help`/`achievements` 覆盖层被 Enter 穿透** —— 在玩法说明界面按 Enter 会直接开始游戏 | `main.js` `handleAction('confirm')` 只看 `mode`，而这两个覆盖层打开时 `mode` 仍是 `menu` | 状态机建模不完整 | `handleAction` 先看 `screens.activeScreen`，处于这两个覆盖层时 Esc/Enter 只返回主菜单 |
| 6 | **点击"成就"抛 `ReferenceError`** | `screens.js` 的按钮回调调用 `renderAchievements()`，但该函数只作为返回对象的方法存在，不在作用域内 | 作用域错误 | 提取为闭包内具名函数 |
| 7 | **主菜单"吸引模式"是死代码** | `startAttract()` 只挂了 AI 却没 `newGame()`，世界停在 `idle`：场上没有食物、没有陨石、玩家蛇没有身体，菜单背景其实是静止的 | 漏了初始化 | `startAttract()` 真正开一局并交给 AI，死亡后自动重开；菜单事件走 `silent` 模式（不发音、不弹提示、不计入本局统计） |

> 第 1、2、6、7 条是**审计员没报、由端到端冒烟测试或我方复核发现**的：说明"单测全绿"不等于"功能完整" —— 4 条缺陷正好都落在测试盲区里。

### 已修复的 P1 摘要

- **AI 卡住误判**：`moved < radius × 0.5` 与速度无关，被玩家的减速力场笼罩（速度减半）或体型变大时会误判为卡住并原地急转 → 改为与"本决策周期预期位移"比较。
- **逐帧新建渐变**：背景线性/径向渐变、竞技场内圈渐变每帧新建 —— 与"热路径零分配"自相矛盾 → 按主题 + 尺寸/半径缓存。
- **`prefers-reduced-motion` 未接入**：只杀了 CSS 动画，JS 侧的相机震屏与粒子照旧 → 系统偏好作为默认值，并让"减弱动效"真正关掉粒子与震屏/闪光。
- **死导出 / 死配置**：`easeOutCubic`、`easeInOutQuad`、`clearGlowCache` 从未被调用；`CONFIG.particles` 声称 1500/750/240 而实际硬编码 900/460/170（改配置完全无效）→ 删除死代码，粒子上限收敛为唯一来源。
- **重复实现**：`formatTime()` 在 `hud.js` 与 `screens.js` 各写一份；道具文案在 `main.js` 与 `hud.js` 各写一份 → 抽出 `core/format.js` 与 `POWERUP_INFO`。
- **`_respawnBot` 兜底不安全**：60 次尝试全失败会沿用最后一次随机点（可能落在陨石里）。
- **`#seg-theme` 主题值无白名单**：非法 theme 会让菜单没有任何高亮项 → 过白名单校验。
- **`isTouch` 被任意触摸永久置真**：触屏笔记本上"鼠标按住左键加速"会失效 → 改按当前指针类型判断。
- **`AudioContext` 构造未 try/catch**、**canvas `textAlign`/`textBaseline` 未复位**、**DPR 变化不触发 resize**、**`--muted` 对比度约 4.0:1 低于 WCAG AA**、**缺少 `user-select: none`**、**缺 `.gitignore`/`README.md`**、**根目录堆了 6 个自查日志**。

---

## 第二轮：2 个全新审计代理（修复验证 / 回归与新漏洞）

第二轮的任务是"**不信注释、只看代码**"，逐条复核上表修复是否真实完整，并主动寻找修复引入的新缺陷。

| 审计员 | 结论 | 对第一轮修复的验证 |
| --- | --- | --- |
| 逻辑与回归 | P0 ×0，P1 ×0，P2 ×1 | 加速扣质量 ✅ / 护盾生效 ✅ / 幽灵语义与文案一致 ✅ / AI 卡住判据 ✅ / 复活兜底 ❌（不完整） |
| 交互与端到端 | P0 ×0，P1 ×1，P2 ×5 | 触屏转向 ✅ / `globalAlpha` ✅ / 覆盖层语义 ✅ / 成就渲染 ✅ / 吸引模式无副作用 ✅ |

第二轮发现并已处理：

- **P1**：`user-select: none` 实际并未写入 CSS 文件（第一轮的编辑丢了）→ 已补。
- **P2**：`_respawnBot` 的兜底候选没有再做一次安全复核 → 追加确定性的网格扫描兜底，保证不会把 AI 放进陨石内部。
- **P2**：新增的 `foodGemValuePerOrb` 没有接到掉落逻辑上（配了也不生效）→ 已接线。
- **P2**：`reset()` 把 `effects.shield` 写成布尔 `false`，与"次数 0/1"的语义不一致 → 统一为 `0`。
- **P2**：`shakeOffset()` 每帧返回新对象、`hud.update()` 每帧 `new Set()`、`evaluateAchievements()` 每帧返回新数组、渐变缓存 key 每帧拼字符串 → 全部改为复用/数值比较。
- **P2**：触屏死区注释写"CSS 像素"但实际按世界单位比较 → `applyPlayerInput` 改为直接在屏幕空间比较（相机变换无旋转，方向等价），死区语义变正确且更省算。
- 审计员误报 1 处：`#overlays` 被判定为死 id，实际 `main.css:268` 在用 → 保留。

### 第二轮之后新增的防护网

审计反复证明"少写一个 `export`"这类问题**单元测试与 `node --check` 都抓不到，但会让整个模块图求值失败、页面白屏**。因此把 `tools/check-syntax.mjs` 从"只做语法检查"升级为"**语法 + 具名导入/导出链接**"双重检查：逐个解析 `import { a, b as c } from './x.js'` 并核对目标模块是否真的导出了这些名字。这一层在开发过程中真的拦下过 2 次白屏级错误。

---

## 最终验证状态

```
npm run check    34 个文件 / 94 条具名导入链接 / 0 失败
npm test         73 / 73 通过（新增 8 个针对本轮缺陷的回归测试）
npm run smoke    50 / 50 通过，hardFail = 0，softFail = 0，无 console 报错
```

冒烟测试覆盖：启动无脚本错误 → 主菜单渲染 → 开始游戏 → 键盘转向/加速 → 触屏按下接管与拖动转向 → 鼠标交互 → Esc 暂停且世界冻结 → 恢复 → 切主题且强调色同步 → 回主菜单 → 吸引模式世界在推进 → 覆盖层 Esc/Enter 不穿透 → 等自然死亡验证结算链路 → 再来一局重置 → 画布确有内容 → 无未捕获异常。
