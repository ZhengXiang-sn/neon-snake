# 独立上下文审计报告（三轮独立审计 + 一轮交付校验 + 一轮迭代版审计）

审计方式：把代码交给**全新上下文、未参与编写**的审计代理，只读、禁止修改文件，要求给出 `文件:行号` 级别的证据，并按 P0 / P1 / P2 分级。

- **P0**：功能不完整、崩溃、明显 bug、与需求不符 —— 必须修，修完要再开一轮审计
- **P1**：边界情况错误、设计缺陷、测试缺口 —— 应当修
- **P2**：风格、可读性、微小优化

第一、二轮为**代理独立审计**；第二轮结束后 **P0 = 0**，用户要求的"直到没有 P0 为止"条件达成。
第三轮为**提交后复跑的稳定性复核**（由我方驱动，非代理），针对端到端测试自身的确定性。
第四轮为**上线产物校验**（非代码审计，属交付校验）。
第五轮是**迭代版审计**：用户反馈驱动的大改造（四种生态 / 地形生成器 / 程序化场景）落地后，新增了三个模块并重写了两个渲染模块，因此**重开两个全新上下文的审计代理**，不沿用旧结论。

---

## 第一轮：3 个审计代理并行（逻辑 / 前端 / 冗余）

| 审计员 | 结论 |
| --- | --- |
| 逻辑与正确性 | P0 ×2，P1 ×3，P2 ×5 |
| 前端与体验 | P0 ×0，P1 ×4，P2 ×10 |
| 冗余与架构 | P0 ×0，P1 ×4，P2 ×11 |

### 确认为真并已修复的 P0

| # | 问题 | 证据（修复前） | 根因 | 修复 |
| --- | --- | --- | --- | --- |
| 1 | **加速完全不消耗质量** —— 需求写明的"用质量换速度"从未实现，按住加速可永久白嫖 1.6× 速度 | `config.js` 定义了 `boostMassCost: 9` 却全仓无读取方；`snake.js` 的 `step()` 只改速度不扣质量 | 定义了配置项但没接线 | `snake.js` `step()`：boosting 时 `mass -= boostMassCost * dt`，触底 `minMassToBoost` 自动松开；补 3 个回归测试 |
| 2 | **护盾道具是纯装饰** —— 拿了不会免死，碰撞判定从不检查它 | `_resolveCollisions()` 只看 `ghost` 与自撞豁免 | 同上，`effects.shield` 被写但从未被读 | `snake.js` 新增 `consumeShield()`（消耗充能 + 0.9s 无敌帧 `grace`）；`_resolveCollisions()` 四种死因统一先尝试护盾；补 2 个测试 |
| 3 | **触屏完全无法转向（手机不可玩）** —— 输入层只在 `pointerType === 'mouse'` 时才切到指针模式，手机玩家永远停在键盘模式、`steerAxis` 恒为 0，蛇只会直走 | `input.js` 的 `onPointerMove/onPointerDown` 里有 `if (e.pointerType === 'mouse')` 才切模式 | 触屏路径漏了 | 触摸事件立即接管转向；死区改为屏幕像素比较；补端到端断言 |
| 4 | **`globalAlpha` 泄漏 → 蛇头/宝石/道具核心半透明** | `glow.js` 的 `drawGlow()` 只赋值不复位 `globalAlpha`，调用方随后的实心填充全部以 0.34~0.55 的 alpha 绘制 | 未恢复画布状态 | `drawGlow()` 保存并还原 `globalAlpha` |
| 5 | **`help`/`achievements` 覆盖层被 Enter 穿透** —— 在玩法说明界面按 Enter 会直接开始游戏 | `main.js` `handleAction('confirm')` 只看 `mode`，而这两个覆盖层打开时 `mode` 仍是 `menu` | 状态机建模不完整 | `handleAction` 先看 `screens.activeScreen`，处于这两个覆盖层时 Esc/Enter 只返回主菜单 |
| 6 | **点击"成就"抛 `ReferenceError`** | `screens.js` 的按钮回调调用 `renderAchievements()`，但该函数只作为返回对象的方法存在，不在作用域内 | 作用域错误 | 提取为闭包内具名函数 |
| 7 | **主菜单"吸引模式"是死代码** | `startAttract()` 只挂了 AI 却没 `newGame()`，世界停在 `idle`：场上没有食物、没有陨石、玩家蛇没有身体，菜单背景其实是静止的 | 漏了初始化 | `startAttract()` 真正开一局并交给 AI，死亡后自动重开；菜单事件走 `silent` 模式（不发音、不弹提示、不计入本局统计） |

> 第 1、2、6、7 条是**审计员没报、由端到端冒烟测试或我方复核发现**的：说明"单测全绿"不等于"功能完整" —— 4 条缺陷正好都落在测试盲区里。

### 已修复的 P1 摘要

- **AI 卡住误判**：`moved < radius × 0.5` 与速度无关，被玩家的减速力场笼罩（速度减半）或体型变大时会误判为卡住并原地急转 → 改为与"本决策周期预期位移"比较。
- **逐帧新建渐变**：背景线性/径向渐变、竞技场内圈渐变每帧新建 —— 与"热路径零分配"自相矛盾 → 按主题 + 尺寸/半径缓存。
- **`prefers-reduced-motion` 未接入**：只杀了 CSS 动画，JS 侧的相机震屏与粒子照旧 → 系统偏好作为默认值，并让"减弱动效"真正关掉粒子与震屏/闪光。
- **死导出 / 死配置**：`easeOutCubic`、`easeInOutQuad`、`clearGlowCache` 从未被调用；`CONFIG.particles` 声称 1500/750/240 而实际硬编码 900/460/170（改配置完全无效）→ 删除死代码，粒子上限收敛为唯一来源。
- **重复实现**：`formatTime()` 在 `hud.js` 与 `screens.js` 各写一份；道具文案在 `main.js` 与 `hud.js` 各写一份 → 抽出 `core/format.js` 与 `POWERUP_INFO`。
- **`_respawnBot` 兜底不安全**：60 次尝试全失败会沿用最后一次随机点（可能落在陨石里）。
- **`#seg-theme` 主题值无白名单**：非法 theme 会让菜单没有任何高亮项 → 过白名单校验。
- **`isTouch` 被任意触摸永久置真**：触屏笔记本上"鼠标按住左键加速"会失效 → 改按当前指针类型判断。
- **`AudioContext` 构造未 try/catch**、**canvas `textAlign`/`textBaseline` 未复位**、**DPR 变化不触发 resize**、**`--muted` 对比度约 4.0:1 低于 WCAG AA**、**缺少 `user-select: none`**、**缺 `.gitignore`/`README.md`**、**根目录堆了 6 个自查日志**。

---

## 第二轮：2 个全新审计代理（修复验证 / 回归与新漏洞）

第二轮的任务是"**不信注释、只看代码**"，逐条复核上表修复是否真实完整，并主动寻找修复引入的新缺陷。

| 审计员 | 结论 | 对第一轮修复的验证 |
| --- | --- | --- |
| 逻辑与回归 | P0 ×0，P1 ×0，P2 ×1 | 加速扣质量 ✅ / 护盾生效 ✅ / 幽灵语义与文案一致 ✅ / AI 卡住判据 ✅ / 复活兜底 ❌（不完整） |
| 交互与端到端 | P0 ×0，P1 ×1，P2 ×5 | 触屏转向 ✅ / `globalAlpha` ✅ / 覆盖层语义 ✅ / 成就渲染 ✅ / 吸引模式无副作用 ✅ |

第二轮发现并已处理：

- **P1**：`user-select: none` 实际并未写入 CSS 文件（第一轮的编辑丢了）→ 已补。
- **P2**：`_respawnBot` 的兜底候选没有再做一次安全复核 → 追加确定性的网格扫描兜底，保证不会把 AI 放进陨石内部。
- **P2**：新增的 `foodGemValuePerOrb` 没有接到掉落逻辑上（配了也不生效）→ 已接线。
- **P2**：`reset()` 把 `effects.shield` 写成布尔 `false`，与"次数 0/1"的语义不一致 → 统一为 `0`。
- **P2**：`shakeOffset()` 每帧返回新对象、`hud.update()` 每帧 `new Set()`、`evaluateAchievements()` 每帧返回新数组、渐变缓存 key 每帧拼字符串 → 全部改为复用/数值比较。
- **P2**：触屏死区注释写"CSS 像素"但实际按世界单位比较 → `applyPlayerInput` 改为直接在屏幕空间比较（相机变换无旋转，方向等价），死区语义变正确且更省算。
- 审计员误报 1 处：`#overlays` 被判定为死 id，实际 `main.css:268` 在用 → 保留。

### 第二轮之后新增的防护网

审计反复证明"少写一个 `export`"这类问题**单元测试与 `node --check` 都抓不到，但会让整个模块图求值失败、页面白屏**。因此把 `tools/check-syntax.mjs` 从"只做语法检查"升级为"**语法 + 具名导入/导出链接**"双重检查：逐个解析 `import { a, b as c } from './x.js'` 并核对目标模块是否真的导出了这些名字。这一层在开发过程中真的拦下过 2 次白屏级错误。

---

## 第三轮：提交后重跑，揪出 1 个真实展示 bug + 3 处测试抖动

首次提交 `d638478` 之后按要求复跑冒烟，出现 **1 红**。**没有当成偶发跳过**，而是连跑统计，发现 4 次里红了 3 个**不同**的条目 —— 说明不是单点缺陷，而是"断言本身不确定"。

### 归因过程（先排除假设，再下结论）

1. 最像的假设是"自动暂停竞态"：`main.js` 在 `visibilitychange` 与 `window blur` 时会 `pause()`，无头环境截图/失焦可能误触发。挂上事件记录器重跑 6 次 → **`vis=[]`、blur 一次都没触发**，假设被排除。
2. 改写高频复现脚本（`.artifacts/probe.mjs`，共 30 轮"暂停→继续"，每 16ms 采样 `mode`）：**4/30 轮出现 `playing → over`**。结论明确 —— 暂停会把世界冻在半空，若蛇正在朝陨石/对手冲，**恢复后几百毫秒内死亡完全正常**，而原断言要求"恢复后仍存活"，等价于要求"这一局刚好没撞上东西"。

### 本轮确认的真实产品缺陷（P1）

| # | 问题 | 证据（修复前） | 根因 | 修复 |
| --- | --- | --- | --- | --- |
| 1 | **分数会以脏浮点直出给玩家** | `world.js:523` `const per = total / count`（宝石按"总价值 ÷ 球数"均分），`world.js:591` 再乘连击倍率；而 `hud.js:148`、`screens.js:136/149/150` 全部 `String(...)` 直出 → 面板可能出现 `73.33333333333333` | 展示层没有对内部浮点值取整 | 新增 `core/format.js` 的 `formatScore()`；HUD / 结算 / 主菜单最高分三处统一调用；`tests/format.test.js` 加 2 条回归测试 |

### 本轮修正的测试缺陷（不是产品的错）

| 断言 | 问题 | 改法 |
| --- | --- | --- |
| `可继续游戏` | 断言"蛇还活着"，依赖随机地形 → 抖动 | 改断言**机制**：轮询至 `mode !== 'paused'` 且 `elapsed` 前进。死亡同样证明世界已解冻，因此不再依赖运气 |
| `AI 对手数量符合难度预设` | 断言"此刻恰好存活 4 个"，而对手撞陨石后需延迟重生 | 快照新增配置常量 `botTarget`；改断言 `botTarget === 4 && 1 ≤ aliveBots ≤ botTarget` |
| `结算面板数值有效（soft）` | `/^\d+$/` 撞上浮点分数 | 由 `formatScore()` 从根上消除 |

> 教训：**端到端断言不能断言随机世界在某一时刻的具体态**，只能断言"机制/不变量"。第一、二轮关注"代码有没有实现功能"，这一轮补上的是"测试本身是否确定"。

## 第五轮：迭代版（生态 / 地形 / 场景）审计

第二轮之后 **P0 = 0**。本轮的触发点是上线后的用户反馈：画面朴素、三个地图只换配色、障碍物集中在中心且过密。改造引入了三个新模块（`game/terrain.js`、`render/scenery.js`、`render/obstacles.js`）并重写了 `render/theme.js` 与 `render/renderer.js` —— **新增这么大体量就不能沿用上一轮的结论**，于是再开两个全新上下文的审计代理，分别审"地形与游戏逻辑"和"渲染与性能"。

| 审计员 | 结论 |
| --- | --- |
| 逻辑与正确性（terrain / world / config / hud） | **P0 ×0**，P1 ×0，P2 ×5 |
| 渲染与性能（scenery / obstacles / renderer / theme） | **P0 ×0**，**P1 ×2**，P2 ×2 |

### 确认为真并已修复的 P1

| # | 问题 | 证据（修复前） | 根因 | 修复 |
| --- | --- | --- | --- | --- |
| 1 | **"减少动效"只关掉了一半** —— 环境粒子、装饰摇摆、竞技场旋转刻度环在 `prefers-reduced-motion` 下照旧运动 | `setReducedMotion` 只写渲染器自身的 `reducedMotion`；`scenery.js` 的 `createScenery()` 根本不接收该标志；`drawArena` 的 `spin`/缩圈脉冲也没读它 | 新增动效时忘记接入无障碍开关 | 场景层新增 `setReducedMotion`（粒子干脆不生成、装饰保留景物只去掉摇摆）；`drawArena` 冻结相位与闪烁。**并补了端到端断言**：用 CDP 模拟系统偏好 + 重载页面，断言 `reducedMotion=true`、`ambientCount=0`、`decorDrawn=decorCount` |
| 2 | **改画质对装饰层完全无效** —— 自动降级（正是为低端机设计的路径）不起作用 | `scenery.setQuality()` 只重建了 `ambient`，`decor` 数组完全没动；于是 `decorLow: 0` / `decorMedium: 70` 配了等于没配 | 漏接线 | 装饰一律按**最高画质**散布，由 `decorLimit` 按当前画质裁剪绘制量。这样降级立即生效，且"调低再调高"不会让装饰永久变少（旧写法会）。补端到端断言"低画质的绘制量必须小于高画质" |

### 已修复的、有实际影响的 P2

| # | 问题 | 证据（修复前） | 修复 |
| --- | --- | --- | --- |
| 1 | **缩圈吞没中途"断供"** —— 缩到约 2/3 处之后，圈继续缩但地形已经没得吃了（观感断裂）。量化：neon 有约 36s、crystal 约 58s（占缩圈时长 20%/32%）是空转 | 外圈按**面积均匀**采样 → 径向密度 ∝ r → 越靠终局圈越稀。审计员实测 120 种子的"最后一次吞没半径"为 neon≈971、crystal≈1052，而终局半径是 840 | 新增**贴身环**（`sentinelRing`，四生态共用）：紧贴终局圈外侧补一圈障碍；并把各布局外圈内边界收敛到终局圈附近、冰晶环改为 7 环等距。结果：末端空档 186 → **84**（≤110 断言），中段空档 201 → **138**（≤170 断言）。同时把"吞没连续性"提升为 `analyzeTerrain` 的输出与单测不变量 |
| 2 | **缩圈中切生态会在圈外凭空生成地形**，下一帧又被一次性吞掉（切换瞬间"圈外炸出一团溶解粒子"） | `rebuildTerrain` → `_generateObstacles` 用的是写死的 `arenaRadiusStart`，没有传当前 `arenaRadius` | 抽出 `_sweepTerrainOutside(R)` 供"缩圈吞没"与"换生态清场"共用（避免两处几何判定漂移），`rebuildTerrain` 生成后按当前半径静默清扫。补单测：缩圈 60% 后切生态，圈外不得有任何障碍物，且不计入 `terrainEaten` 统计 |
| 3 | **食物可能被永久埋进石头里** —— 重试次数用尽后直接返回"最后一次被挡住的点" | `_foodSpot` 的 `return p` 落在与障碍物重叠的位置；内圈石头终局也不会溶解，埋在里面的食物整局吃不到，而开局的 240 颗普通食物只播种一次、不会重掷 | 新增 `_blockedAt`，重试用尽后改为**按同心环确定性扫描**一个真正空闲的落点。把既有断言的容差从 `inside <= 4` 收紧为 `inside === 0`，并补一条强制走兜底分支的单测 |
| 4 | **地形随机源与玩法随机源共用一支 rng**（维护性脆弱点） | `generateTerrain(this.biome, this.rng, …)` 直接消费 `this.rng`，之后 AI / 掉落 / 食物继续用它。当前确定（地形是第一个消费方），但改任何地形参数都会静默改变下游随机序列，且测试锁不住 | 地形改用独立派生源 `(seed ^ hash(biome))`。附带好处：同一 (种子, 生态) 恒产出同一张图，来回切换生态不再"每切一次换一张脸"。补单测：消耗玩法 rng 500 次后地形不变；A→B→A 地形完全一致 |
| 5 | **`innerClear` 的语义被夸大** | 配置注释写"保证出生时四周无遮挡"，但 `findSpot` 只保证障碍**中心**不进 300；实测本体最近可到距原点 ~272 | 修正注释，明确是"中心净空"，并写明实测本体不会盖住原点 |

### 顺带修掉的两个"逐帧拼字符串"（与"热路径零分配"约定冲突）

- `drawSky` / `drawVignette` 每帧拼 `` `${themeId}|${w}x${h}` `` 作为渐变缓存 key → 改为数值比较。
- 闪光与出圈警示每帧拼 `rgba(...)` → 改为 `globalAlpha` 控制透明度；闪光颜色字符串改在 `camera.pulse()` 触发**那一刻**合成一次；警示色提为模块常量 `EDGE_PRESSURE_COLOR`。

### 审计员验证过、确认没问题的点（避免下一轮重复劳动）

- **墙是原子级整道存亡**：`_dissolveTerrain` 按 `reach` 整组判定/整组移除，不会留半截墙把圆形场地切成互不连通的两块（这是设计上最怕的失效模式）。
- **越界死亡与地形无关**：由 `arenaRadius` 独立判定，因此不存在"从缺口走出圈"。
- **出生点永不被围死**：300 种子统计 `containsOrigin = 0`；所有障碍都在开局圈内，必会被缩圈吞到。
- **无 `shadowBlur` / `filter`**；`createLinearGradient` / `createRadialGradient` 只出现在预渲染与按尺寸缓存的路径里。
- **画布状态成对恢复**：`setLineDash` 有配对复位、`ctx.ellipse` 在 `save/restore` 内、`lighter` 逐处复原，无泄漏导致全屏变亮/永久虚线。
- **`seamlessTile` 的无缝性成立**：3×3 偏移补齐跨边界图元，且 `bg` 只填一次（避免早期"9 遍叠加致 alpha 涨到 0.93"的死黑）。
- **墙体视觉与碰撞一致**：墙用单一 `r`（不是变半径），视觉 `lineWidth = r*2` 的圆头折线恰好等于沿折线的圆盘并集，与逐节点圆判定等价。

### 本轮顺带修掉的测试自身缺陷

`tests/terrain.test.js` 里"墙体节点顺序连续"的循环写成 `for (let i = 1; i < nodes[i1(nodes)]; i++)`，而 `i1()` 返回 `nodes.length` → `nodes[nodes.length]` 是 `undefined` → **断言恒不执行，是个死循环体**。已修正为 `i < nodes.length`，并顺手把判据从 `d < r * 1.4` 收紧为 `d <= r * 2`（相邻节点间距达到直径时，渲染出的墙上会出现肉眼可见的缺口）。

---

---

## 第四轮：上线产物校验（非代码审计，属交付校验；针对第一版上线）

本机出口为白名单代理，`*.vercel.app` 不可直连，因此不能"打开线上页面"来验收。改用 Vercel API 做等价校验：

| 校验项 | 方法 | 结果 |
| --- | --- | --- |
| 部署状态 | `GET /v13/deployments/{id}` | `state=READY`，`target=production` |
| 是否走了构建 | 同上 | `buildSkipped=False`、`builds=0`（纯静态，无构建步骤） |
| 生产别名 | 同上 `alias` | `neon-snake-kappa.vercel.app`、`neon-snake-zheng-xiang.vercel.app` |
| 文件完整性 | `GET /v6/deployments/{id}/files` 遍历树 | 50 个文件，无缺失、无多余 |
| **内容一致性** | `GET /v7/deployments/{id}/files/{uid}` 取回 base64 → 与本地逐一比对 SHA-256 | **50/50 哈希全等** |

结论：**线上 artifact 与本地"通过 check 0 失败 / 75 单测 / 5×50 端到端"的那棵树逐字节相同**。据此可判定上线产物正确；唯一未覆盖的是真人浏览器的目视确认（受本机出口限制）。

---

## 最终验证状态

```
npm run check    37 个文件 / 115 条具名导入链接 / 0 失败
npm test         108 / 108 通过
npm run smoke    连跑 3 次全绿：61 / 61，hardFail = 0，softFail = 0，无 console 报错
```

**单元测试覆盖（纯逻辑层）**：转向与惯性、质量↔速度/半径/转向率、加速烧质量、自撞豁免、护盾/磁铁/幽灵/减速力场、连击倍率、成就判定、空间网格碰撞、AI 寻路与重生安全、竞技场缩圈、**地形生成的四项不变量 + 缩圈吞没节奏 + 随机源隔离 + 食物落点兜底**、分数格式化、同种子可复现、长时间空跑不抛异常。

**端到端覆盖（真实页面）**：启动无脚本错误 → 主菜单渲染 → 开始游戏 → 键盘转向/加速 → 触屏按下接管与拖动转向 → 鼠标交互 → Esc 暂停且世界冻结 → 恢复 → **逐生态巡检（4 种生态的地形指纹与画面指纹两两不同）** → 回主菜单 → 吸引模式世界在推进 → 覆盖层 Esc/Enter 不穿透 → 等自然死亡验证结算链路 → 再来一局重置 → 画布确有内容 → **模拟系统"减少动效"偏好并重载，验证"空气粒子归零 / 景物保留 / 低画质真的降绘制量"** → 无未捕获异常。

**这轮审计最值钱的一条**：地形分布这类"看起来改了、其实只改了一半"的问题，光靠自查很难发现 —— 第一版改造已经把平均半径从 26% 提到 55%，看上去很像解决了，但**缩圈吞没会在中途断供**这件事，是靠审计员用 120 个种子做量化统计才浮出来的。把它变成可断言的指标（`dissolveGap` / `dissolveTail`），才真正锁住了这个性质。
